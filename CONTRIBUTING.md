# 贡献指南

欢迎在本 fork 的 [Issues](https://github.com/YiuTerran/emqx-oss/issues) 提交问题，或通过 Pull Request 提交修复。请说明受影响的版本、复现步骤、预期行为和实际行为。安全漏洞请按[安全报告说明](./SECURITY.md)私密提交，不要在公开 Issue 中披露细节。

本仓库以 EMQX `v5.8.9` 社区版为基线。提交应聚焦社区版源码和构建流程，不引入上游 BSL 许可代码或已经移除的企业版应用。修改行为时请补充相应测试，并说明执行结果及尚未验证的环境。

## 提交信息

提交标题使用 `<类型>(<可选范围>): <简短说明>`；类型可用 `feat`、`fix`、`docs`、`refactor`、`perf`、`test`、`build`、`ci`、`chore` 或 `revert`。标题描述实际改动，单行不超过 100 个字符。需要解释原因时，在空行后的正文中说明；关联 Issue 可写在正文末尾。

示例：

```text
fix(session): 清理失效的会话注册表记录

避免节点退出后仍保留无法恢复的会话条目。

Closes: #123
```

## 变更记录

当前发布脚本仍读取 `changes/ce/` 下的 `*.en.md` 变更条目。需要发布说明的社区版功能或修复，按现有文件命名约定添加 `feat-<PR编号>.en.md`、`perf-<PR编号>.en.md` 或 `fix-<PR编号>.en.md`。历史变更记录和发布脚本沿用此约定。
